{
  "name": "vite-react-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c \"npm run dev\"",
    "build": "npx @tinacms/cli build --local && tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.1",
    "@tinacms/cli": "latest",
    "framer-motion": "^12.35.0",
    "lucide-react": "^0.400.0",
    "tinacms": "^3.14.0"
  }
}